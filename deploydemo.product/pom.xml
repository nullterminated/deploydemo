<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>deploydemo</groupId>
		<artifactId>deploydemo</artifactId>
		<version>1.0.4-SNAPSHOT</version>
	</parent>
	<artifactId>deploydemo.product</artifactId>
	<packaging>eclipse-repository</packaging>
	<properties>
		<product-id>deploydemo.product</product-id>
		<maven.install.skip>true</maven.install.skip>
		<maven.deploy.skip>true</maven.deploy.skip>
	</properties>
	<build>
		<plugins>
			<plugin>
				<groupId>org.eclipse.tycho</groupId>
				<artifactId>tycho-p2-repository-plugin</artifactId>
				<configuration>
					<includeAllDependencies>true</includeAllDependencies>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.eclipse.tycho</groupId>
				<artifactId>tycho-p2-director-plugin</artifactId>
				<configuration>
					<profile>deploydemo.product</profile>
					<products>
						<product>
							<id>${product-id}</id>
							<rootFolder>deploydemo.product</rootFolder>
						</product>
					</products>
				</configuration>
				<executions>
					<execution>
						<id>materialize-products</id>
						<goals>
							<goal>materialize-products</goal>
						</goals>
					</execution>
					<execution>
						<id>archive-products</id>
						<goals>
							<goal>archive-products</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>exec-maven-plugin</artifactId>
				<executions>
					<execution>
						<id>generate-shasums</id>
						<phase>package</phase>
						<goals>
							<goal>exec</goal>
						</goals>
						<configuration>
							<workingDirectory>${project.build.directory}/products/</workingDirectory>
							<outputFile>${project.build.directory}/products/sha512sums.txt</outputFile>
							<executable>sh</executable>
							<commandlineArgs>-c 'sha512sum -b ${project.artifactId}-*'</commandlineArgs>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-deploy-plugin</artifactId>
				<executions>
					<execution>
						<phase>deploy</phase>
						<goals>
							<goal>deploy-file</goal>
						</goals>
						<configuration>
							<groupId>${project.groupId}</groupId>
							<artifactId>${project.artifactId}</artifactId>
							<version>${project.version}</version>
							<generatePom>true</generatePom>
							<packaging>sha512sums</packaging>
							<file>${project.build.directory}/products/sha512sums.txt</file>
							<files>${project.build.directory}/products/${project.artifactId}-linux.gtk.x86_64.tar.gz,${project.build.directory}/products/${project.artifactId}-macosx.cocoa.x86_64.tar.gz,${project.build.directory}/products/${project.artifactId}-win32.win32.x86_64.zip</files>
							<classifiers>linux.gtk.x86_64,macosx.cocoa.x86_64,win32.win32.x86_64</classifiers>
							<types>tgz,tgz,zip</types>
							<repositoryId>github</repositoryId>
							<url>https://maven.pkg.github.com/${github.repository}</url>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>